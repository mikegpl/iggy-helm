apiVersion: v1
kind: Service
metadata:
  name: {{ include "iggy-rs.fullname" . }}
  labels:
    {{- include "iggy-rs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iggy-rs.selectorLabels" . | nindent 4 }}
